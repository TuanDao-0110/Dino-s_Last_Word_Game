import { createSlice, PayloadAction } from "@reduxjs/toolkit";

const initialState = {};

const ScoreSlicer = createSlice({
  name: "score",
  initialState: initialState,
  reducers: {},
});

export const {} = ScoreSlicer.actions;
export default ScoreSlicer.reducer;
